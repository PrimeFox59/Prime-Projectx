# Firebase Credentials (JSON string)
# Download your service account JSON from Firebase Console
# For local: Place it as 'firebase-credentials.json' in the project root
# For Vercel: Copy entire JSON content to FIREBASE_CREDENTIALS environment variable

SECRET_KEY=your-secret-key-here
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Example for Vercel environment variable FIREBASE_CREDENTIALS:
# Paste the entire content of firebase-credentials.json as-is (minified or not)

